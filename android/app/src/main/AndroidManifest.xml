<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- [NEW 2026-08-21 / 개편 2026-08-28] AdMob 앱 ID. 이 meta-data가 없으면 앱이
             시작하자마자 크래시한다(SDK가 강제한다). 값은 하드코딩하지 않고 gradle
             manifestPlaceholder(admobAppId)로 주입한다 — android/local.properties 의
             admobAppId 가 있으면 실 ID, 없으면 구글 공식 테스트 앱 ID로 폴백한다.
             (build.gradle 상단 로더, src/platform/android/adIds.ts 참고) -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="${admobAppId}" />

    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- [NEW 2026-08-21] 햅틱(navigator.vibrate) — 없으면 진동이 조용히 무시된다.
         normal 수준 권한이라 런타임 요청이 필요 없다. -->
    <uses-permission android:name="android.permission.VIBRATE" />
</manifest>
